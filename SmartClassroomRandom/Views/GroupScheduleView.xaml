<UserControl x:Class="SmartClassroomRandom.Views.GroupScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2B2D35" CornerRadius="12" Padding="15" Margin="0,0,0,20">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">

                <Border Background="#1E1E24" CornerRadius="8" Padding="15,10" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Tổng số nhóm: " Foreground="#A0A0A5" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding TotalGroups}" Width="40" Background="Transparent" Foreground="White" 
                                 BorderThickness="0" TextAlignment="Center" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Border>

                <Border Background="#1E1E24" CornerRadius="8" Padding="15,10" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Số ngày báo cáo: " Foreground="#A0A0A5" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding TotalDays}" Width="40" Background="Transparent" Foreground="White" 
                                 BorderThickness="0" TextAlignment="Center" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Border>

                <Button Content="📅 Xếp Lịch Ngẫu Nhiên" Command="{Binding GenerateScheduleCommand}"
                        Foreground="White" FontWeight="Bold" FontSize="15"
                        Padding="25,10" BorderThickness="0" Cursor="Hand">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#00B4DB" Offset="0.0" />
                            <GradientStop Color="#0083B0" Offset="1.0" />
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ScheduleDays}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2B2D35" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding DayName}" 
                                           Foreground="#B465DA" FontSize="20" FontWeight="Bold"/>

                                <Rectangle Grid.Row="1" Height="1" Fill="#404040" Margin="0,10,0,15"/>

                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Groups}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#32343E" BorderBrush="#4A90E2" BorderThickness="1" 
                                                    CornerRadius="8" Padding="15,10" Margin="0,0,10,10">
                                                <TextBlock Text="{Binding}" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>