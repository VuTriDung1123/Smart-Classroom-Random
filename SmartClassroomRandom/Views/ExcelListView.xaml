<UserControl x:Class="SmartClassroomRandom.Views.ExcelListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2B2D35" CornerRadius="12" Padding="20,15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📋 Danh sách sinh viên" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text=" (Tổng hợp)" Foreground="#A0A0A5" FontSize="18" VerticalAlignment="Bottom" Margin="5,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#2B2D35" CornerRadius="12" Padding="15">
            <DataGrid ItemsSource="{Binding Students}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      IsReadOnly="True"
                      Background="Transparent" 
                      RowBackground="#2B2D35" 
                      AlternatingRowBackground="#32343E"
                      Foreground="White"
                      BorderThickness="0" 
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#404040"
                      HeadersVisibility="Column"
                      RowHeight="40">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E1E24"/>
                        <Setter Property="Foreground" Value="#A0A0A5"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="10,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#404040"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3B82F6"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT / ID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Tên Sinh Viên" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Phát biểu" Binding="{Binding PhatBieu}" Width="100"/>
                    <DataGridTextColumn Header="Không trả lời" Binding="{Binding KhongTraLoiDuoc}" Width="120"/>
                    <DataGridTextColumn Header="Vắng mặt" Binding="{Binding KhongDiHoc}" Width="100"/>
                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="150"/>
                </DataGrid.Columns>

            </DataGrid>
        </Border>
    </Grid>
</UserControl>